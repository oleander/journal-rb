<%= simple_form_for([@project, @post]) do |f| %>
  <%= f.input :started_at, discard_minute: true %> 
  <%= f.input :ended_at, discard_minute: true %>
  <%= f.input :body, :as => :text %>
  <%= f.button :submit %>
<% end %>